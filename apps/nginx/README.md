# Nginx App Snippets

This directory is intentionally optional.

Put per-app nginx server snippets here when an app needs a custom reverse proxy config.
If no snippets are needed, it can stay as-is with this README.
